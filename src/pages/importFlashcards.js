import { pageStyle } from '../utils/styles'

const ImportFlashcards = () => {
	return (
		<div style={pageStyle}>
			<h1 style={{ 'text-align': 'center' }}>Import Flashcards</h1>
			<p style={{ 'text-align': 'center' }}>This is the page to import flashcards.</p>
		</div>
	);
};

export default ImportFlashcards;